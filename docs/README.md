# Docs Status

Documentation has been intentionally reduced while the project surface is being simplified.

What remains here right now:
- `config-examples/default-config.toml` (bundled into the app as the default config)

Command help and subcommand descriptions are currently maintained directly in Swift source files.
